<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vetserveph">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PetOwner.petListView"></activity>
        <activity android:name=".PetOwner.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PetOwner.accountInformation" />
        <activity android:name=".PetOwner.petProfile" />
        <activity android:name=".PetOwner.addPet" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCAwIw4Yo8I11xVFe98hAw4LBrSg4HDxy8" />

        <activity android:name=".locateClinic" />
        <activity android:name=".clinicInformation" />
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PetOwner.personalInformation" />
        <activity android:name=".PetOwner.Login" />
    </application>

</manifest>